global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "s2s-api"
    metrics_path: "/metrics"
    static_configs:
      - targets: ["api:8000"]
    scrape_interval: 5s

  - job_name: "node-exporter"
    static_configs:
      - targets: ["node-exporter:9100"]
